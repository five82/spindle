// Package stageexec provides one-shot stage execution for CLI workflows
// (e.g., "spindle workflow identify"). It wraps a single stage.Handler
// invocation with in_progress bookkeeping and structured logging.
package stageexec
